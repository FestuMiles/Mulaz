module.exports = {
  content: ['./views/**/*.ejs'],
  theme: {
    extend: {
      fontFamily: {
        sans: ['Poppins', 'ui-sans-serif', 'system-ui']
      }
    }
  }
}
// tailwind.config.js
// This is a Tailwind CSS configuration file that sets up the content paths and extends the theme with custom font families.